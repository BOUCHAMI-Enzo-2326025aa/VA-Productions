# Backend (Render / local)
# Ne jamais committer un vrai .env avec secrets.

# MongoDB Atlas
MONGODBURL=mongodb+srv://<USER>:<PASSWORD>@<CLUSTER_HOST>/<DB_NAME>?retryWrites=true&w=majority

# JWT
SECRET=<LONG_RANDOM_SECRET>

# CORS + liens email (doit matcher l'origin Vercel)
FRONT_LINK=https://<your-frontend>.vercel.app

# Port (en local seulement - Render le fournit automatiquement)
PORT=5555

# Emails (Brevo)
BREVO_API_KEY=<BREVO_API_KEY>
BREVO_SENDER_EMAIL=<sender@domain.com>
BREVO_SENDER_NAME=V.A. Productions

# Google Calendar (serveur)
GOOGLE_CLIENT_ID=<google-oauth-client-id>
GOOGLE_CLIENT_SECRET=<google-oauth-client-secret>
# Optionnel: si absent, le serveur utilise automatiquement https://<host>/api/google/callback
GOOGLE_REDIRECT_URI=

# Factur-X
VAPRODUCTIONS_SIRET=<SIRET>
VAPRODUCTIONS_VAT=<VAT_NUMBER>
