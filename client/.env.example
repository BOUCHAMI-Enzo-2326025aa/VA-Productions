# Frontend (Vercel / local)
# Toutes les variables VITE_* sont publiques (injectées dans le bundle).

# URL API backend (Render)
VITE_API_HOST=https://<your-backend>.onrender.com

# Google Calendar (client)
VITE_CLIENT_ID_CALENDAR=<google-oauth-client-id>
VITE_API_KEY_CALENDAR=<google-api-key>
VITE_CALENDAR_ID=primary
VITE_SCOPES=https://www.googleapis.com/auth/calendar.events
